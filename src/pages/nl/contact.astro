---
// TODO(review-nl): Nederlandse vertaling van de Contact-pagina.

import BaseLayout from '../../layouts/BaseLayout.astro';
import Button from '../../components/ui/Button.astro';
import Input from '../../components/ui/Input.astro';
import Textarea from '../../components/ui/Textarea.astro';
import Link from '../../components/ui/Link.astro';
import Tag from '../../components/ui/Tag.astro';

const turnstileSiteKey = import.meta.env.PUBLIC_TURNSTILE_SITE_KEY ?? '';

// TODO(copy): echte Cal.com / agendalink invullen zodra deze is opgezet.
const calendarUrl = 'https://cal.com/ahmet-karabas';
const contactEmail = 'ahmetkarabasdtengineer@gmail.com';
const linkedinUrl = 'https://linkedin.com/in/ahmetkarabas3917';
---

<BaseLayout
  title="Contact"
  description="Plan een gesprek van 30 minuten, of schrijf ons eerst. Wij zeggen eerlijk in het gesprek of we de juiste match zijn."
>
  {
    turnstileSiteKey && (
      <Fragment slot="head">
        <script is:inline src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer />
      </Fragment>
    )
  }

  <section class="mx-auto max-w-7xl px-6 pt-24 pb-16 md:pt-32">
    <p class="font-mono text-xs uppercase tracking-wider text-accent">Contact</p>
    <h1 class="mt-6 max-w-4xl font-display text-5xl leading-[1.05] tracking-tight md:text-7xl">
      Laten we praten.
    </h1>
    <p class="mt-8 max-w-2xl text-lg leading-relaxed text-ink-muted md:text-xl">
      Vertel ons wat duur, traag of onduidelijk is. Wij zeggen eerlijk of we de juiste match zijn &mdash; in het gesprek, niet na drie mails.
    </p>
  </section>

  <section class="border-t border-line/60 bg-surface">
    <div class="mx-auto max-w-7xl px-6 py-20 md:py-24">
      <div class="grid gap-12 md:grid-cols-12 md:items-center">
        <div class="md:col-span-7">
          <Tag variant="accent">Snelste route</Tag>
          <h2 class="mt-6 font-display text-3xl leading-tight tracking-tight md:text-5xl">
            Plan een gesprek van 30 minuten.
          </h2>
          <p class="mt-6 max-w-xl text-base leading-relaxed text-ink-muted md:text-lg">
            Geen slides. Geen salesscript. Kies een moment dat past, vertel ons het ene ding dat wrijving geeft, en wij zeggen hoe een zinvolle eerste week eruitziet &mdash; of dat we niet de juiste match zijn.
          </p>
        </div>
        <div class="flex flex-col gap-4 md:col-span-5 md:items-end">
          <Button href={calendarUrl} size="lg" external>
            Kies een moment
          </Button>
          <p class="font-mono text-[10px] uppercase tracking-wider text-ink-muted">
            Agenda opent in een nieuw tabblad
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="border-t border-line/60 bg-bg">
    <div class="mx-auto max-w-7xl px-6 py-20 md:py-28">
      <div class="grid gap-12 md:grid-cols-12">
        <div class="md:col-span-5">
          <p class="font-mono text-xs uppercase tracking-wider text-ink-muted">Of schrijf eerst</p>
          <h2 class="mt-6 font-display text-3xl leading-tight tracking-tight md:text-4xl">
            Liever een bericht?
          </h2>
          <p class="mt-6 max-w-md text-base leading-relaxed text-ink-muted">
            Schrijf ons kort wat wrijving geeft. Wij reageren binnen twee werkdagen &mdash; meestal dezelfde dag &mdash; met een voorgestelde afspraak of met een eerlijk &ldquo;wij zijn hier niet de juiste match.&rdquo;
          </p>
          <div class="mt-6 space-y-2 text-sm text-ink-muted">
            <p class="font-mono text-[10px] uppercase tracking-wider">Direct</p>
            <p>
              <Link href={`mailto:${contactEmail}`}>{contactEmail}</Link>
            </p>
            <p>
              <Link href={linkedinUrl} variant="external">LinkedIn</Link>
            </p>
          </div>
        </div>

        <div class="md:col-span-7">
          <form
            id="contact-form"
            method="POST"
            action="/api/contact"
            class="flex flex-col gap-6 border border-line bg-surface p-6 md:p-10"
            novalidate
            data-contact-form
          >
            <div aria-hidden="true" class="absolute -left-[10000px] top-auto h-px w-px overflow-hidden">
              <label>
                Laat dit veld leeg.
                <input type="text" name="website" tabindex="-1" autocomplete="off" />
              </label>
            </div>

            <div class="grid gap-6 md:grid-cols-2">
              <Input id="contact-name" name="name" label="Naam" required autocomplete="name" />
              <Input
                id="contact-email"
                name="email"
                label="E-mail"
                type="email"
                required
                autocomplete="email"
              />
            </div>
            <Input
              id="contact-organization"
              name="organization"
              label="Organisatie"
              hint="Optioneel &mdash; helpt ons voorbereid te komen."
              autocomplete="organization"
            />
            <Textarea
              id="contact-message"
              name="message"
              label="Waar zit de wrijving?"
              required
              rows={6}
              placeholder="Een paar zinnen is genoeg. De rekening groeit, het dashboard klopt niet, dit duurde vroeger een dag in plaats van vier &mdash; dat soort dingen."
            />

            {
              turnstileSiteKey ? (
                <div class="cf-turnstile" data-sitekey={turnstileSiteKey} data-theme="dark" />
              ) : (
                <p class="font-mono text-[10px] uppercase tracking-wider text-ink-muted">
                  Anti-spam-controle is in deze omgeving uitgeschakeld.
                </p>
              )
            }

            <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
              <Button type="submit" size="lg">
                <span data-submit-idle>Versturen</span>
                <span data-submit-busy hidden>Wordt verstuurd&hellip;</span>
              </Button>
              <p class="font-mono text-[10px] uppercase tracking-wider text-ink-muted">
                Wij lezen alles. Geen nieuwsbrief.
              </p>
            </div>

            <p
              role="status"
              aria-live="polite"
              class="hidden text-sm"
              data-form-message
            />
          </form>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  const form = document.querySelector<HTMLFormElement>('[data-contact-form]');
  if (form) {
    const submitBtn = form.querySelector<HTMLButtonElement>('button[type="submit"]');
    const idleLabel = form.querySelector<HTMLElement>('[data-submit-idle]');
    const busyLabel = form.querySelector<HTMLElement>('[data-submit-busy]');
    const statusEl = form.querySelector<HTMLElement>('[data-form-message]');

    const setBusy = (busy: boolean) => {
      if (submitBtn) submitBtn.disabled = busy;
      if (idleLabel) idleLabel.hidden = busy;
      if (busyLabel) busyLabel.hidden = !busy;
    };

    const setStatus = (kind: 'success' | 'error' | null, text: string) => {
      if (!statusEl) return;
      if (!kind) {
        statusEl.classList.add('hidden');
        statusEl.textContent = '';
        return;
      }
      statusEl.classList.remove('hidden');
      statusEl.className =
        kind === 'success' ? 'text-sm text-accent' : 'text-sm text-ink';
      statusEl.textContent = text;
    };

    form.addEventListener('submit', async (event) => {
      event.preventDefault();
      setStatus(null, '');
      setBusy(true);

      try {
        const formData = new FormData(form);
        const resp = await fetch('/api/contact', {
          method: 'POST',
          body: formData,
          headers: { Accept: 'application/json' },
        });
        const data = (await resp.json()) as { ok: boolean; error?: string };

        if (resp.ok && data.ok) {
          setStatus('success', 'Dank u — wij reageren binnen twee werkdagen.');
          form.reset();
          const cf = (window as unknown as { turnstile?: { reset: () => void } }).turnstile;
          cf?.reset();
        } else {
          setStatus(
            'error',
            data.error ?? 'Er ging iets mis. Probeer het opnieuw of stuur ons direct een e-mail.',
          );
        }
      } catch {
        setStatus(
          'error',
          'Netwerkfout. Controleer uw verbinding en probeer opnieuw, of stuur ons direct een e-mail.',
        );
      } finally {
        setBusy(false);
      }
    });
  }
</script>
